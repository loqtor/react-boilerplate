{
  "name": "**APP_TITLE**-dev-proxy",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently --kill-others 'yarn start:server' 'cd client; yarn start'",
    "install:all": "yarn install; cd client; yarn install; cd ..",
    "start:server": "nodemon ./bin/www --ignore 'client/*'",
    "post-yarn": "sh ./tools/yarn-scripts/yarn.sh"
  },
  "dependencies": {
    "debug": "^3.1.0",
    "express": "^4.16.4",
    "http-proxy": "^1.17.0",
    "morgan": "^1.9.0",
    "serve-favicon": "^2.5.0",
    "uuid": "^3.2.1"
  },
  "devDependencies": {
    "concurrently": "^4.1.0",
    "nodemon": "^1.17.3"
  }
}
